#include "main.h"
/**
 * mul - a function that multiplies two integers.
 * @a: first argument
 * @b: second argument
 * Return: multiply of a and b
 */
int mul(int a, int b)
{
	int mult = (a * b);

	return (mult);
}
