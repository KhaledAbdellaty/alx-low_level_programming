#include "main.h"
/**
 * _isdigit - a function that checks for a digit
 * returns 1 if argument is a digit otherwise returns 0
 * @c: an argument
 * Return: return 1 if argument is a digit
 * otherwise returns 0.
 */
int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
