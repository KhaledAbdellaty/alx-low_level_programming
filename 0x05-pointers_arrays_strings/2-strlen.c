#include "main.h"
/**
 * _strlen - a function that returns the length of a string
 * @s: is a parameter
 */

int _strlen(char *s)
{
	int i;
	int x;

	for (i = 0; *(s + i) != 0; i++)
	{
		x = i;
	}
	return (i);
}
